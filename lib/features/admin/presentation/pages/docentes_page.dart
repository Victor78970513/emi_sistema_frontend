import 'package:flutter/material.dart';

class DocentesPage extends StatelessWidget {
  const DocentesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello, World! Docentes'),
      ),
    );
  }
}
